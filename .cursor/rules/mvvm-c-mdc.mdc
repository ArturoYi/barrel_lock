---
description: Riverpod 状态管理规范
globs: "**/*.dart"
alwaysApply: false
---

# MVVM-C 页面模块

## 分层与包归属

| 层 | 位置 | 包 |
|---|---|---|
| **V** View | 各平台 `lib/pages/<feature>/` | 各平台 app |
| **VM** ViewModel | `apps/barrel_lock/lib/features/<feature>/` | `barrel_lock` |
| **M** Model | 同上 | `barrel_lock` |
| **C** Coordinator | 同上 | `barrel_lock` |

参考实现：`launch_screen` — View 在各平台 `lib/pages/`，M/VM/C 在 `barrel_lock`。

## barrel_lock Feature 目录

```
features/<feature>/
├── <feature>.dart
├── <feature>_view_model.dart
├── <feature>_coordinator.dart
└── <feature>_model.dart
```

| 层 | 允许 | 禁止 |
|---|---|---|
| **View** | `ref.watch` 渲染、`ref.read` 转发用户事件 | `AppRouter`、Repository、复杂业务逻辑 |
| **ViewModel** | 调用 Model、触发 Coordinator、暴露状态/命令 | 直接操作 Navigator、`BuildContext` |
| **Coordinator** | `AppRouter.push/go/pop` 等导航 | 业务状态、UI 构建 |
| **Model** | 数据读写、异步初始化 | Widget、路由 |

## 与本项目栈的映射

- **VM 状态**：Riverpod 3.x `Notifier` / `AsyncNotifier`，import `package:core/core.dart`
- **共享 Feature**：`package:barrel_lock/barrel_lock.dart`
- **C 层路由**：`AppRoutes` + `AppRouter`（`package:barrel_lock/barrel_lock.dart`）
- **平台差异**：通过 Riverpod `override` 注入（如 `launchScreenPrepareProvider`）
