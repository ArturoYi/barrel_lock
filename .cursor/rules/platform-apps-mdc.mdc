---
alwaysApply: false
---

# 平台拆分约定

- 每个平台是独立 Flutter 工程（BarrelLock_android / _ios / _macos …）
- 共享：`core`（基础设施）、`barrel_lock`（业务 M/VM/C + 路由壳）、`barrel_lock_ui`（启动入口与根 Widget）
- 各平台 `lib/main.dart`：调用 `runBarrelLockApp(configureRouter: ...)`，通过 `overrides` 注入平台差异
- **路由**：`AppRoutes` / `AppRouter` 在 `barrel_lock`；`configureBarrelLockRouter()` 在各平台 `lib/router/app_router_config.dart`
- **页面 View**：各平台 `lib/pages/`
- 禁止在 app 层直接改 fast_navigator 内部实现
