---
description: 多平台 App 结构规范
globs: "apps/**"
alwaysApply: false
---

# 平台拆分约定

- 每个平台是独立 Flutter 工程（BarrelLock_android / _ios / _macos …）
- 共享：`core`（基础设施 + 路由）、`barrel_lock`（业务 M/VM/C）
- 各平台 `lib/main.dart`：`configureBarrelLockRouter()` → `ProviderScope` → `ThemedApp.router`
- **路由**：`AppRoutes` / `AppRouter` 在 `core`；`lib/router/app_router_config.dart` 注入 Page
- **页面**：`lib/pages/` 仅保留 View（MVVM-C 的 V 层）；M/VM/C 在 `barrel_lock`
- 禁止在 app 层直接改 fast_navigator 内部实现
