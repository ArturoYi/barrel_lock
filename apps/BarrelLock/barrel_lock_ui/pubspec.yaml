name: barrel_lock_ui
description: BarrelLock app shell (启动入口与根 Widget)。
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  barrel_lock:
  core:

dev_dependencies:
  build_runner: ^2.12.0
  flutter_gen_runner: ^5.14.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter_gen:
  assets:
    outputs:
      package_parameter_enabled: true

flutter:
  assets:
    - assets/app_icon_center.png
