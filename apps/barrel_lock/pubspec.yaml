name: barrel_lock
description: BarrelLock shared business features (MVVM-C ViewModel / Model / Coordinator).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  app_l10n:
    path: ../../packages/app_l10n
  core:
  fast_dialog:
    path: ../../packages/fast_dialog
  fast_navigator:
    path: ../../packages/fast_navigator
  fast_lifecycle:
    path: ../../packages/fast_lifecycle
  fast_toast:
    path: ../../packages/fast_toast
  fast_loading:
    path: ../../packages/fast_loading
  url_launcher: ^6.3.2

dev_dependencies:
  cryptography: ^2.9.0
  drift: ^2.34.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  shared_preferences: ^2.5.5

flutter:
